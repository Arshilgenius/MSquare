<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.arshilgenius.msquare">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.USE_CREDENTIALS" />


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".orderActivity"
            android:label="ORDERING PORTAL" />
        <activity
            android:name=".virtualbp"
            android:label="Virtual BP" />
        <activity
            android:name=".About_us"
            android:label="About Us" />
        <activity
            android:name=".BMIActivity"
            android:label="Body Mass Index" />
        <activity
            android:name=".DepressionActivity"
            android:label="FIRST AID" />
        <activity
            android:name=".Burns"
            android:label="Burns First Aid" />
        <activity
            android:name=".fainting"
            android:label="Fainting  First Aid" />
        <activity
            android:name=".fracture"
            android:label="Fracture First Aid" />
        <activity
            android:name=".heartattack"
            android:label="HeartAttack First Aid" />
        <activity
            android:name=".sprain"
            android:label="Sprain First Aid" />
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />
    </application>

</manifest>