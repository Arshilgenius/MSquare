<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.arshilgenius.msquare">
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".orderActivity"
            android:label="ORDERING PORTAL" />
        <activity
            android:name=".virtualbp"
            android:label="Virtual BP" />
        <activity
            android:name=".About_us"
            android:label="About US" />
        <activity
            android:name=".BMIActivity"
            android:label="Body Mass Index" />
        <activity android:name=".DepressionActivity"
            android:label="Depression Calculator"/>
    </application>
</manifest>